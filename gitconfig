[user]
	name = Katie Delfin
	email = yassskatie@github.com
[github]
	user = yassskatie
[push]
	default = simple
[credential]
	helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[alias]
	lol = log --graph --decorate --pretty=oneline --abbrev-commit --all
	s = status -s
  mustash = stash -u
	sweep = diff master --name-status --color -- app/
	pr = pull-request
[color]
	ui = true
[core]
	editor = vim
